# 部署進度與未解決事項

更新日期：2026-09-10。

## 尚未解決：外部 App 連線

學校防火牆申請、HTTPS 憑證及反向代理尚未完成，手機或專研筆電到學校 API 的完整連線尚未驗證。
VPN／遠端桌面可以登入，不代表 App 已能連到 API。不得將此事項標記完成，直到取得核准、完成設定並實測。

目前 API 維持 127.0.0.1:3000。正式對外存取規劃為核准的 TCP 443；不將 MySQL 3307 或 Node 3000 直接公開。
不因繼續開發其他功能，就繞過學校網路限制或透過公開 HTTP 傳輸會員密碼。

## 已確認

- 使用者截圖確認 Windows 服務 SchoolFoodAppApi 為 Running、Auto、LocalService，健康檢查通過。
- 已設定開機自動啟動，但尚未收到伺服器實際重開機後的驗證結果。
- 原會員登入與偏好資料庫測試曾在學校 MySQL 通過。

## 會員活動後端已驗收

- MySQL 收藏、最近 20 筆瀏覽紀錄、模擬訂單 API；只存取目前登入會員的資料。
- 下單使用資料庫價格、庫存鎖定、交易與識別碼防重送；保存下單當時餐點及營養資料快照。
- 使用者 2026-09-10 截圖確認：學校伺服器已更新至 2edb68d，db:check 通過，002 所需欄位與索引已就緒。
- 伺服器 npm.cmd test：22 項通過、2 項 MySQL 整合測試跳過；接續 npm.cmd run test:mysql：8 項全部通過、無跳過，涵蓋真實資料庫會員隔離、持久化、快照及並行下單。
- 更新後 SchoolFoodAppApi 已重新啟動，Running、Auto、LocalService，127.0.0.1:3000 健康檢查通過。這不是伺服器重開機驗證，也不是外部 App 連線驗證。
- 遷移前 mysqldump 匯出成功，備份檔為 foodapp-20260910-191210.sql（19,179 bytes）；尚未驗證還原或異機備份。

## 商品目錄後端已驗收

- 使用者 2026-09-10 截圖確認：學校伺服器已更新至 cd6859d，db:check 通過。
- npm.cmd test：25 項通過、2 項 MySQL 整合測試跳過；接續 npm.cmd run test:mysql：8 項全部通過、無跳過，包含新增的商品目錄 SQL 與可見性驗證。
- SchoolFoodAppApi 已恢復 Running、Auto、LocalService，127.0.0.1:3000 健康檢查通過。
- 使用者回報商品 API 顯示 {"items":[],"nextCursor":null}，表示目前查詢沒有符合上架條件的商品，不代表連線失敗，也不能據此判定所有資料表都沒有資料。
- Flutter Cloud Catalog (Debug) 已實作，但手機到學校 API 的實際連線與畫面仍未驗收。沒有自動把展示用的 100 項餐點匯入 MySQL。

## App 雲端會員活動已實作，後端更新已驗收

- CLOUD_CATALOG=true 登入後讀寫雲端收藏、瀏覽與模擬訂單，等待伺服器確認才顯示成功。
- 401 清除失效登入；切換帳號後捨棄舊請求的畫面更新，避免混入其他會員資料。
- 待確認訂單先持久保存 UUID 與品項，斷線或重開 App 後用原識別碼重試，不自行宣告訂單失敗或另建訂單。
- 訂單採用伺服器價格及不可變快照，初次載入 20 筆並可分頁；會員統計僅涵蓋已載入模擬訂單。
- 使用者提供的更新後終端輸出確認：學校伺服器由 cd6859d 快轉更新至 d7156d8，npm ci 成功，db:check 確認 shan_jie_ren_yi、3307、food_app@localhost。
- 更新後 npm.cmd test 為 25 項通過、2 項真實 MySQL 測試跳過；npm.cmd run test:mysql 為 8 項通過、0 失敗、0 跳過，包含訂單快照 originalUnitPrice 新增斷言。
- Stop、更新、Start 後服務恢復 Running、Auto、LocalService；127.0.0.1:3000 健康及程序歸屬檢查通過。服務程序 PID 1916、API PID 8340 僅為該次驗收快照。
- 本機測試涵蓋 App 流程，學校後端更新已驗收；真機或跨裝置同步仍未驗收，服務重啟不等於整台伺服器重開機驗證。見 [雲端活動驗證](cloud_activity_verification.md)。
- 本次沒有資料庫遷移，勿重新匯入 schema.sql 或 001、002。

## 接續開發

1. 完成正式商家帳號及上架管理，讓已授權商家建立商品，不由一般會員任意上架。
2. 完成外部 HTTPS 連線後，驗證雲端商品畫面、兩個裝置同步、不同會員隔離、斷線重試不重複下單。
3. 安排伺服器重開機驗證自動啟動，並驗證資料庫備份還原與異機備份。

App 預設仍為展示目錄及本機活動；雲端模式才串接會員活動，購物車、搜尋紀錄及評分回饋仍為本機資料。不能宣稱跨裝置同步已實測完成。
雲端模式使用相同資料庫 ID 提供首頁、搜尋、推薦、轉盤與超商資料，下單由 API 確認。資料庫沒有商品時不混入展示餐點，也不自動匯入測試商品。
尚未整合金流或超商訂單；模擬訂單不代表已付款或商家已接單。
