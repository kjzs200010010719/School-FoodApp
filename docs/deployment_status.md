# 部署進度與未解決事項

更新日期：2026-09-10。

## 尚未解決：外部 App 連線

學校防火牆申請、HTTPS 憑證及反向代理尚未完成，手機或專研筆電到學校 API 的完整連線尚未驗證。
VPN／遠端桌面可以登入，不代表 App 已能連到 API。不得將此事項標記完成，直到取得核准、完成設定並實測。

目前 API 維持 127.0.0.1:3000。正式對外存取規劃為核准的 TCP 443；不將 MySQL 3307 或 Node 3000 直接公開。
不因繼續開發其他功能，就繞過學校網路限制或透過公開 HTTP 傳輸會員密碼。

## 已確認

- 使用者截圖確認 Windows 服務 SchoolFoodAppApi 為 Running、Auto、LocalService，健康檢查通過。
- 已設定開機自動啟動，但尚未收到伺服器實際重開機後的驗證結果。
- 原會員登入與偏好資料庫測試曾在學校 MySQL 通過。

## 本次新增，尚待雲端驗收

- MySQL 收藏、最近 20 筆瀏覽紀錄、模擬訂單 API；只存取目前登入會員的資料。
- 下單使用資料庫價格、庫存鎖定、交易與識別碼防重送；保存下單當時餐點及營養資料快照。
- 學校伺服器尚需套用 002_member_activity.sql，並執行 npm.cmd run test:mysql。
- 本機測試使用替身驗證 API 與交易邏輯；不能取代真實 MySQL 的 SQL、並行與持久化測試。

## 接續開發

1. 在學校伺服器完成本次遷移與 MySQL 整合測試。
2. 將商品目錄串接 MySQL，建立 App 與資料庫一致的商品 ID；保留模擬資料與正式資料的界線。
3. App 串接收藏、瀏覽與訂單 API，加入等待、失敗、重試與登入失效狀態。
4. 完成外部 HTTPS 連線後，驗證兩個裝置同步、不同會員隔離、斷線重試不重複下單。

目前 App 的收藏、瀏覽、購物車與購買紀錄仍在本機，不能宣稱已完成跨裝置同步。
尚未整合金流或超商訂單；模擬訂單不代表已付款或商家已接單。
